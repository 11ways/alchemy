/**
 * The BelongsToFieldType class
 *
 * @constructor
 *
 * @author   Jelle De Loecker   <jelle@develry.be>
 * @since    0.2.0
 * @version  0.2.0
 */
var BelongsToFieldType = Function.inherits('ObjectIdFieldType', function BelongsToFieldType(schema, name, options) {
	BelongsToFieldType.super.call(this, schema, name, options);

	// @todo: set index stuff
});
